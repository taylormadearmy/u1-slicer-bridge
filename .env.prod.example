# U1 Slicer Bridge — Optional environment overrides
# Copy to .env to customize your deployment

# Web UI port (default: 8080)
# WEB_PORT=8080

# Database credentials (default: u1slicer/u1slicer)
# POSTGRES_USER=u1slicer
# POSTGRES_PASSWORD=u1slicer
# POSTGRES_DB=u1slicer

# Printer connection (optional — can also be set in the UI under Settings)
# MOONRAKER_URL=http://192.168.1.100:7125
